name: Unit Tests

on:
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2
    - name: Setup Miniconda
      uses: s-weigand/setup-conda@v1
    - name: Install dependencies
      run: |
        conda install -c anaconda pyqt
        conda install -c conda-forge pybind11 pytest
    - name: Build Project
      run: |
        mkdir build
        cd build
        cmake .. -DPYTHON_LIBRARY_DIR=C:/Miniconda/lib/site-packages/ -DPYTHON_EXECUTABLE=C:/Miniconda/python.exe -G "Visual Studio 15 2017 Win64"
        cmake --build . --config Release
    - name: Install NBodySimulations
      run: |
        dir
        cmake --install .
    - name: Run Python Unit Tests
      run: |
        pytest tests/unit_tests/python/
