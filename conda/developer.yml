name: n-body-env

channels:
  - conda-forge

dependencies:
  - ccache
  - cmake
  - cppcheck
  - coverage<5
  - coveralls
  - flake8
  - gmock  # Required due to gtest referring to a missing gmock library
  - gtest
  - ninja
  - pre-commit
  - pytest
  - pytest-mock
  - python=3.8.*
